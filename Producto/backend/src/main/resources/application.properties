# Base de datos PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/inventario_db
spring.datasource.username=inventario_user
spring.datasource.password=inventario123
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Servidor
server.port=8080

# Desactivar seguridad temporalmente para desarrollo

# JWT
jwt.secret=claveSuperSecretaParaElProyectoDeInventarioQueDebeSerMuyLarga2024Ruby
jwt.expiration=86400000

